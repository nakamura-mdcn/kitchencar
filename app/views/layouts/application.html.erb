<!DOCTYPE html>
<html lang="ja">
  <head>
    <title>KitchenCar</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    
    <header class="header">
      <div class="header__bar">
        <h1 class="header__title"><a href="/">KitchenCar</a></h1>
      </div>
      <ul class="user__info">
        <span>
          <div class="user__name">
            <%= "ログイン中ユーザー名" %>
          </div>
          <li>
            <%# <%= link_to "マイページ", "/" %>
            <div class="user__icon">
              <%= link_to "ログアウト", "/", data: { turbo_method: :delete } %>
              <%= link_to "投稿する", "/", class: "post" %>
              <%= link_to "ログイン", "/", class: "post" %>
              <%= link_to "新規登録", "new_user_registration", class: "post" %>
            </div>
          </li>
        </span>
      </ul>
  </header>

    <%= yield %>

    <footer>
      <p>
        Copyright.
      </p>
    </footer>

  </body>
</html>
