<!DOCTYPE html>
<html>
  <head>
    <title>KitchenCar</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="header__title"><a href="/">KitchenCar</a></h1>
        <%# <% if user_signed_in? %> 
          <div class="user_nav">
            <span><%= "ログイン中ユーザー名" %>
              <ul class="user__info">
                <li>
                  <%= link_to "マイページ", "/" %>
                  <%= link_to "ログアウト", "/", data: { turbo_method: :delete } %>
                </li>
              </ul>
            </span>
            <%= link_to "投稿する", "/", class: "post" %>
          </div>
        <%# <%else%> 
          <div class="user_nav">
            <%= link_to "ログイン", "/", class: "post" %>
            <%= link_to "新規登録", "/", class: "post" %>
          </div>
        <%# <%end %> 
      </div>
    </header>

    <%= yield %>

    <footer>
      <p>
        Copyright.
      </p>
    </footer>

  </body>
</html>
